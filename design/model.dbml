// Use DBML to define your database structure
// Docs: https://dbml.dbdiagram.io/docs

Table F_Applicant {
  id number(16,0) [pk, unique, increment]
  first_name varchar(20) [not null]
  middle_name varchar(20)
  last_name varchar(20) [not null]
  email varchar(50)
  phone varchar(20)
  WeChat varchar(20)
  LinkedIn varchar(20)
  address_street varchar(100)
  address_suite_number varchar(10)
  address_city varchar(50)
  address_postal_code varchar(20)
  address_province varchar(20)
  address_country varchar(20)
  created_by varchar(20)
  modified_by varchar(20)
  created_at date
  modified_at date
}

Table Applicant_Skills {
  applicant_id number(16,0) [ref: > F_Applicant.id]
  skill_id number(16,0) [ref: > F_Skills.id]
}

Table F_Skills {
  id number(16,0) [pk, unique, increment]
  category varchar(20)
}

Table F_Application {
  id number(16,0) [pk, unique, increment]
  applicant_id number(16,0) [ref: > F_Applicant.id]
  role varchar(20)
  status varchar(10)
  created_by varchar(20)
  modified_by varchar(20)
  created_at date
  modified_at date
}

Table F_Document {
  id number(16,0) [pk, unique, increment]
  application_id number(16,0) [ref: > F_Application.id]
  content blob
  created_by varchar(20)
  modified_by varchar(20)
  created_at date
  modified_at date
}

// Ref: F_Applicant.id <> F_Skills.id // many-to-many

